airsim
numpy>=1.19
matplotlib>=3.3
pyyaml>=5.4
torch>=1.9.0
gymnasium>=0.28.0
tqdm>=4.62
pandas>=1.3
scipy>=1.7
opencv-python>=4.5
flask>=3.0  # v3 Web 控制台/编辑器后端
pywebview>=5.0  # Windows 原生弹窗（WebView2 内核渲染 Web 控制台）

# Project AirSim (UE 5.7) client requirements
msgpack>=1.0
pynng>=0.7
commentjson>=0.9

# 打包 exe（可选，仅构建时用）: pip install pyinstaller
# 产物由 scripts/tools/build_exe.py 生成（--onefile --noconsole，排除 torch 等重型依赖）
